#include "grade.hpp"
#include "block.hpp"
#include <iostream>

using namespace std;

int main(){

  Grade * gradea = new Block();
  bool grade;

  gradea->setX(3);
  gradea->setY(3);

  gradea->imprimirGrade(grade);


  return 0;
}
