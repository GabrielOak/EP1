#include "grade.hpp"
#include <iostream>

using namespace std;

int main(){

  Grade gradea;
  bool grade;
  gradea.atribuirValor(grade);
  gradea.aplicarRegras(grade);
  gradea.imprimirGrade(grade);


  return 0;
}
